[atomisator]

sources = 
    rss     %(test_dir)s/digg.xml
    rss     %(test_dir)s/tarek.xml
    rss     %(test_dir)s/pp.xml

database = sqlite:///tests/atomisator.db

file = atomisator.xml
title = meta
description = My feed
link = http://link

filters =
   stopwords    %(test_dir)s/stopwords.txt            

[plugins]

xml = atomisator.plugin.xml

